# 🏗️ ML Model Integration Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                    TUNEIQ DASHBOARD (app.py)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         Dashboard Main Sections                          │  │
│  │  • Data Configuration                                    │  │
│  │  • Filters & Analysis                                    │  │
│  │  • Platform Analytics                                    │  │
│  │  • Revenue Gap Analysis                                  │  │
│  │  • Economic Impact Analysis                              │  │
│  └────────────────────────▲─────────────────────────────────┘  │
│                           │                                     │
│                           │ calls                               │
│                           │                                     │
│  ┌────────────────────────▼─────────────────────────────────┐  │
│  │  🤖 AI ECONOMIC IMPACT SECTION (economic_impact.py)     │  │
│  │                                                          │  │
│  │  • Data Source Selection UI                            │  │
│  │  • Artist Name Input                                   │  │
│  │  • Run Prediction Button                               │  │
│  │  • Results Display                                     │  │
│  │  • Data Summary & Preview                              │  │
│  └────────────────────────▲─────────────────────────────────┘  │
│                           │                                     │
│                           │ calls                               │
│                           │                                     │
└─────────────────────────┬─────────────────────────────────────────┘
                          │
                          │ get_model_predictions()
                          │
                   ┌──────▼────────────────────────────────────────┐
                   │   DATA PIPELINE (data_pipeline.py)           │
                   ├────────────────────────────────────────────────┤
                   │                                                │
                   │  1. Load Data:                                │
                   │     • load_sample_data()                      │
                   │     • fetch_live_data(spotify/youtube/web)   │
                   │     • enrich_data_with_web()                 │
                   │                                               │
                   │  2. Call Model:                               │
                   │     get_model_predictions(df)                │
                   │         │                                     │
                   │         └──► predict_impact(df)              │
                   │                                               │
                   │  3. Return Results:                           │
                   │     {'predicted_gdp': X, 'predicted_jobs': Y}│
                   │                                               │
                   └──────┬──────────────────────────────────────────┘
                          │
                          │ predict_impact()
                          │
            ┌─────────────┴──────────────────────────────────────┐
            │                                                    │
      ┌─────▼────────────────────────────────────────────────┐  │
      │  PREDICTOR MODULE (predictor.py)                     │  │
      ├──────────────────────────────────────────────────────┤  │
      │                                                      │  │
      │  1. Load Model                                      │  │
      │     load_tuneiq_model()                            │  │
      │     └──► tuneiq_gdp_jobs_model.joblib              │  │
      │                                                      │  │
      │  2. Prepare Features                                │  │
      │     prepare_features(df)                           │  │
      │     ├─► Extract: streams, countries, platforms    │  │
      │     ├─► Calculate: platform_count                 │  │
      │     ├─► Calculate: avg_streams_per_country         │  │
      │     └─► Return: feature matrix [n, 3]             │  │
      │                                                      │  │
      │  3. Predict                                         │  │
      │     model.predict(X)                               │  │
      │     └─► Returns: [GDP, Jobs]                       │  │
      │                                                      │  │
      │  4. Format & Return                                │  │
      │     {'predicted_gdp': float,                       │  │
      │      'predicted_jobs': float,                      │  │
      │      'confidence': float}                          │  │
      │                                                      │  │
      └──────┬──────────────────────────────────────────────┘  │
             │                                                  │
             │ loads                                           │
             │                                                  │
      ┌──────▼──────────────────────────────────────────────┐  │
      │ tuneiq_gdp_jobs_model.joblib                       │  │
      │ (Pre-trained ML Model)                             │  │
      ├───────────────────────────────────────────────────┤  │
      │ Inputs:                                            │  │
      │  • streams: Total streams/views (float)           │  │
      │  • platform_count: # of platforms (int)           │  │
      │  • avg_streams_per_country: Avg spread (float)    │  │
      │                                                    │  │
      │ Outputs:                                           │  │
      │  • GDP Contribution (₦)                           │  │
      │  • Jobs Created                                    │  │
      └────────────────────────────────────────────────────┘  │
            │                                                  │
            └──────────────────────────────────────────────────┘

           Data Sources                Supported Platforms
           ════════════                ════════════════════
           • Streaming Data            • Spotify
           • Geographic Info           • YouTube
           • Platform Info             • Apple Music
           • Web Scraped Data          • Web Scraper
           
                                     Output Formats
                                     ══════════════
                                     • Currency (₦)
                                     • Numbers with commas
                                     • Percentages
                                     • JSON objects
```

## Data Flow: Example Prediction

```
USER INPUT
─────────
Artist: "Burna Boy"
Source: "Sample"

           │
           ▼
┌──────────────────────────────┐
│ Load Sample Data             │
│ ├─ 147 records               │
│ ├─ 125M streams              │
│ ├─ 45 countries              │
│ └─ 3 platforms               │
└──────────────────┬───────────┘
                   │
                   ▼
┌──────────────────────────────┐
│ Prepare Features             │
│ ├─ streams: 125,000,000      │
│ ├─ platform_count: 3         │
│ └─ avg_streams_per_country:  │
│    2,777,778                 │
└──────────────────┬───────────┘
                   │
                   ▼
┌──────────────────────────────┐
│ Run Model.predict()          │
│ Input: [1.25e8, 3, 2.78e6]  │
└──────────────────┬───────────┘
                   │
                   ▼
┌──────────────────────────────────┐
│ MODEL OUTPUT                     │
│ ├─ GDP: 2,450,000,000 ₦         │
│ ├─ Jobs: 1,250                   │
│ └─ Confidence: 0.92              │
└──────────────────┬────────────────┘
                   │
                   ▼
┌──────────────────────────────────────┐
│ DISPLAY RESULTS (UI)                 │
├───────────────────────────────────────┤
│                                       │
│ ✅ Predictions generated              │
│                                       │
│ 💰 GDP Contribution: ₦2,450,000,000  │
│ 👥 Jobs Created: 1,250                │
│ 🎯 Confidence: 92%                    │
│                                       │
│ 📈 Data Summary                       │
│ • Records: 147                        │
│ • Streams: 125,000,000                │
│ • Countries: 45                       │
│                                       │
└──────────────────────────────────────┘
```

## Component Interaction Map

```
┌─────────────────────┐
│   User (Browser)    │
└──────────────┬──────┘
               │
               │ clicks "Run AI Prediction"
               │
               ▼
        ┌─────────────┐
        │  Streamlit  │  (session state, UI rendering)
        │ (app.py)    │
        └──────┬──────┘
               │
               │ calls display_economic_impact_section()
               │
               ▼
    ┌──────────────────────┐
    │ economic_impact.py   │  (UI rendering, data source selection)
    └──────────┬───────────┘
               │
               │ calls get_model_predictions(df)
               │
               ▼
    ┌──────────────────────┐
    │ data_pipeline.py     │  (data orchestration)
    └──────────┬───────────┘
               │
               ├─ Load Data
               │  ├─ load_sample_data()
               │  ├─ fetch_live_data("spotify")
               │  ├─ fetch_live_data("youtube")
               │  └─ fetch_live_data("web")
               │
               ├─ Calls predict_impact(df)
               │
               ▼
    ┌──────────────────────┐
    │ predictor.py         │  (ML operations)
    └──────────┬───────────┘
               │
               ├─ load_tuneiq_model()
               │  └─ tuneiq_gdp_jobs_model.joblib
               │
               ├─ prepare_features(df)
               │
               ├─ model.predict(X)
               │
               └─► Returns predictions dict
                   │
                   ▼
    ┌──────────────────────┐
    │ economic_impact.py   │  (format results)
    └──────────┬───────────┘
               │
               ├─ format_currency()
               ├─ format_number()
               └─► Display formatted results
                   │
                   ▼
    ┌──────────────────────┐
    │  Streamlit UI        │  (render in browser)
    └──────────┬───────────┘
               │
               ▼
        ┌─────────────┐
        │  Browser    │  (user sees results)
        └─────────────┘
```

## Module Dependencies

```
app.py
  ├── predictor (via economic_impact)
  ├── economic_impact ←──────────────────────┐
  │   └── data_pipeline                      │
  │       └── predictor                      │ imports
  │           └── tuneiq_gdp_jobs_model      │
  └── models                                 │
      └── economic_impact_proxy              │
                                             │
requirements.txt                             │
  └── joblib ───────────────────────────────┘

external modules:
  ├── streamlit (UI)
  ├── pandas (data)
  ├── numpy (arrays)
  └── importlib (dynamic imports)
```

## Error Handling Flow

```
User clicks "Run AI Prediction"
         │
         ▼
    Try to load data
         │
    ┌────┴────┐
    │          │
Success       Error
    │          │
    ▼          ▼
Process   Return empty DataFrame
    │          │
    │          └──► display_economic_impact()
    │               │
    ▼               ▼
Call model    Try predictions
    │              │
┌───┴───┐      ┌────┴────┐
│       │      │          │
OK  Error    Success   Error
│   │        │         │
│   │        ▼         ▼
│   │       Display   Show Error Message
│   │       Results   └─ "Could not predict"
│   │                 └─ "Check your data"
│   │                 └─ "Try sample data"
│   │
│   └──► Return Error Dict
│        {'error': 'message'}
│
└──► Display Results
     ├─ GDP: ₦X
     ├─ Jobs: Y
     └─ Confidence: Z%
```

## Data Transformation Pipeline

```
Raw Input Data
═════════════════════════════════════════════
├─ artist: string
├─ streams: int/float
├─ country: string
├─ platform: string
├─ month: string
├─ region: string
└─ [other columns]
          │
          ▼
    prepare_features()
════════════════════════════════════════════
│
├─ Extract numeric streams
├─ Count unique countries
├─ Count unique platforms
├─ Calculate aggregate stats
│
└─► Feature Matrix
    ═════════════════════════════════════════
    ┌─────────────────────────────────────┐
    │ streams    │ platform_count │ avg... │
    ├─────────────────────────────────────┤
    │ 125000000  │       3        │ 27... │
    └─────────────────────────────────────┘
          │
          ▼
    model.predict(X)
════════════════════════════════════════════
          │
          ▼
    Predictions
    ═════════════════════════════════════════
    [2450000000, 1250]
    [GDP ₦,      Jobs]
          │
          ▼
    Format Results
    ═════════════════════════════════════════
    {
      'predicted_gdp': 2450000000,
      'predicted_jobs': 1250,
      'confidence': 0.92,
      'error': None
    }
          │
          ▼
    Display UI
    ═════════════════════════════════════════
    💰 GDP: ₦2,450,000,000
    👥 Jobs: 1,250
    🎯 Confidence: 92%
```

## Key Integration Points

```
┌─────────────────────────────────────────────────────┐
│ INTEGRATION POINT 1: app.py imports                │
│ Lines 31, 46, 61 - Dynamic module loading          │
│ Imports: display_economic_impact_section            │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ INTEGRATION POINT 2: app.py displays section       │
│ Lines 1659-1661 - Add AI predictions to dashboard │
│ Calls: display_economic_impact_section()            │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ INTEGRATION POINT 3: economic_impact.py calls      │
│ Imports: get_model_predictions from data_pipeline  │
│ Calls: get_model_predictions(df)                    │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ INTEGRATION POINT 4: data_pipeline.py calls        │
│ Imports: predict_impact from predictor             │
│ Calls: predict_impact(df) in get_model_predictions │
└─────────────────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────┐
│ INTEGRATION POINT 5: predictor.py loads            │
│ Loads: tuneiq_gdp_jobs_model.joblib               │
│ Uses: joblib.load() to deserialize model          │
└─────────────────────────────────────────────────────┘
```

---

This architecture ensures:
✅ Clean separation of concerns
✅ Easy testing & debugging
✅ Flexible data source support
✅ Robust error handling
✅ Extensible design for future features

